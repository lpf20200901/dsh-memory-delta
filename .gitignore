# 运行 / 测试产物
# ⚠️ gitignore **不支持行尾注释**：# 只在行首才是注释，写在模式后面会被当成模式的一部分
# （历史踩坑：`.test-sandbox-*/       # 按进程分…` 因此从未生效，测试沙箱被提交进仓库）。
node_modules/
.test-sandbox/
.test-sandbox-*/
.preflight-store/
.tmp-*/
.dbg/
.probe*
*.log

# 安装备份（安装脚本生成的）
*.bak-memory-*
*.bak2-memory-*
*.bak3-memory-*

# 打包产物与临时缓存（npm pack 时用 --cache 指进仓库，免得写到工作区外的 npm-cache）
*.tgz
.npm-cache/
